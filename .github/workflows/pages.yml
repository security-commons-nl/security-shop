name: Deploy mockup naar GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload mockup als Pages-artefact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./mockup

      - name: Deploy naar Pages
        id: deployment
        uses: actions/deploy-pages@v4
